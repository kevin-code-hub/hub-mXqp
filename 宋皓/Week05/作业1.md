客服工作台 作业
https://help.aliyun.com/zh/document_detail/421160.html?spm=a2c4g.11186623.0.0.30f638fe8lQYuY
功能1: faq 类目管理，一级类目，二级类目， 对应的faq 信息
https://help.aliyun.com/zh/document_detail/421161.html?spm=a2c4g.11186623.0.0.30f67558YDiTwC
功能2: faq录入和管理，待选提问、相似提问、生效时间、对应回答
使用场景：代替人工客服，用户的提问直接与历史的提问进行相似度匹配，最相似的提问的回答返回给用户

后端开发：
1.设计并实现如下数据库:
一级类目表、二级类目表（父子关联）,用 parent_id 实现无限级分类
FAQ 主表（绑定一二类目、标准问题、答案、生效 / 失效时间、状态）
待选提问表、相似提问表（关联对应 FAQ）
2.开发类目管理接口：一级 / 二级类目增删改查、排序、启用禁用
3.开发 FAQ 管理接口：录入、编辑、删除、生效时间控制、上下线
4.开发提问匹配接口：接收用户问题 → 调用算法服务 → 返回对应答案
5.负责数据校验、权限、日志、缓存、部署与稳定性

算法开发:
1.基于 BERT 训练 / 微调问题相似度模型
2.搭建向量库，把所有 FAQ 标准问、相似问向量化
3.提供语义匹配接口：输入问题 → 返回最相似 FAQ ID
4.支持相似提问挖掘：自动生成 / 去重相似问
5.可选：提供类目自动预测、大模型增强能力
6.迭代优化效果

需要使用什么模型？

1.BERT语义匹配模型（做主搜）
2.向量检索库（FAISS/Milvus/ElasticSearch 向量）

如何使用bert的？
用法一：生成句向量 (Sentence Embedding)

- 场景：把知识库里的问题变成向量存起来。

- 技术：使用 **Sentence-BERT (SBERT)** 架构。

- 原理：将用户的 Query 和库里的 FAQ 分别输入 BERT，取 `[CLS]` 标记或 `Mean Pooling` 层的输出作为句子的向量表示。

- 目的：计算余弦相似度（Cosine Similarity），找出语义最接近的问题。
用法二：语义匹配分类 (Semantic Matching)

- 场景：判断“用户的问题”和“候选问题”是不是同一个意思。

- 技术：将两个句子拼接成 `[CLS] 句子A [SEP] 句子B [SEP]` 输入 BERT。

- 输出：接一个全连接层（Dense Layer），输出 0 或 1（或是/否的概率）。

- 优缺点：比向量余弦相似度更准，但速度慢，通常用于**精排阶段**。

是否需要使用大模型
按需使用,可作为无匹配时的兜底回答
